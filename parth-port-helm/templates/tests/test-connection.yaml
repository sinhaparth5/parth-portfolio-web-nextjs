apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "parth-port-helm.fullname" . }}-test-connection"
  labels:
    {{- include "parth-port-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "parth-port-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
